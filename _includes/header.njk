{% set items = [
  { 'About': '/about/' },
  { 'Update One': '/updates/one/' }
] -%}

<a class="logo" href="/">eleventy-start</a>

<ul>
{% for item in items -%}
{% for name, link in item -%}
<li><a {% if link in page.url -%}class="active" {% endif -%}{% if 'http' in link -%}target="_blank" rel="noopener" {% endif -%}href="{{ link }}">{{ name }}</a></li>
{% endfor -%}
{% endfor -%}
</ul>
